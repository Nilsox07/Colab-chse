# Kein direkter Web-Zugriff auf das Applikationsverzeichnis (Config, Schema, Helpers).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
